# G1 incident — VM death 2026-05-17

## Summary

The Hot Aisle MI300X VM at `hotaisle@23.183.40.87` (`enc1-gpuvm014`) became
unreachable at approximately **15:30-15:38 UTC** on 2026-05-17.
Almost certainly **budget runout** (the campaign ran ~30h past my original
20:30 UTC May 16 burn-rate estimate — credits either lasted longer than
predicted or were topped up silently).

- Last successful rescue: `15197b6` at 15:08:01Z (V8 pdmrg-gpu-opt, config
  282/296, josephson L=64 χ=256 rep 7/10).
- Last on-VM heartbeat that reached origin: `e92d529` at 13:57:28Z.
- First failed contact: 15:38 UTC (cron poll). SSH connect timeout +
  ping 100% packet loss.

## Data salvaged

Rescue pipeline (cron `05a2cfc0` at :07/:37 every hour + on-VM
`result_backup.sh` every 10 min via configured GITHUB_TOKEN) successfully
captured all results landed up through the last completed rep.

Final V8 (pdmrg-gpu-opt) state at VM death:
- **23/37 configs completed** (~62%) — through josephson L=64 χ=128
  (config 281, full 10 reps)
- **Config 282** (josephson L=64 χ=256): partial, 7/10 reps captured.
  Reps 8-10 were in flight when VM died.
- **TFIM block (configs 283-296, 14 configs) not started**.

Final all-variant state:
- V1-V7 (dmrg-gpu-base/std/opt, dmrg2-gpu-base/std, pdmrg-gpu-base/std,
  pdmrg-gpu) — **all 37 configs each, complete**.
- V8 (pdmrg-gpu-opt) — 23/37 configs complete.
- dmrg2-gpu-opt — **deferred** (D-G1-1 hang, never ran).

KILLs+FAILs through the run: 2 total (1 from May-09 smoke phase
dmrg2-gpu-opt hang, 1 from my manual kill of a stuck V8 config 266 rep
8 on 2026-05-17 02:39 UTC).

## Notable in-session interventions

1. **2026-05-17 02:39 UTC** — Manual SIGTERM of stuck binary PID 1221289
   running V8 config 266 (heisenberg L=200 χ=64) rep 8. Rep had been
   running ~50 min when typical rep was ~120s. Davidson appears to have
   entered an infinite reorthogonalization basin. Kill succeeded;
   launcher recorded `rep 8/10: FAIL` and rep 9 with fresh RNG init
   completed normally in 121s. **Pathology is RNG-dependent, not
   config-deterministic.** This is a softer variant of the D-G1-1
   deferred bug but in pdmrg-gpu-opt rather than dmrg2-gpu-opt.

## Suggested follow-up (for next GPU window)

1. **V8 tail completion**: configs 282 (3 remaining reps), 283-296
   (full 14 tfim configs) = 14.3 configs of pdmrg-gpu-opt left. Use
   `CHI_SKIP=512 VARIANT_ONLY=pdmrg-gpu-opt` plus a `CONFIG_START` filter
   if available, or just re-run the full V8 since data is preserved
   per-config in the JSON.
2. **D-G1-1 fix attempt**: see `reviews/d-g1-1-static-analysis-20260514.md`
   — the static analysis is in place; needs a GPU window to test the
   Step-3 strided-batched port.
3. **χ=512 mop-up**: not started this window (deferred for budget).

## Local cron status

Cron `05a2cfc0` (`7,37 * * * *`) is still scheduled and will continue
firing /g1-poll. Each fire will detect the VM as dead and produce a
brief status note. **Leave the cron in place** — when a new VM is
provisioned, just update `~/.gpu-host` with the new IP, and the rescue
flow will resume automatically.

## Files

- `reviews/g1-vm-logs/g1_full_20260509-1718.log` — final launcher log
- `benchmarks/paper_results/mi300x/g1-single-gpu-20260509-1718/` —
  per-variant JSON files
- `reviews/d-g1-1-static-analysis-20260514.md` — deferred bug analysis
- `reviews/g1-deferred-bugs-20260509.md` — original deferred bug manifest
